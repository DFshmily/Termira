{
  "name": "@termira/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/ipc-schema.js",
  "types": "./dist/ipc-schema.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
